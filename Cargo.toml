[workspace]
members = [
    "zircon-object",
    "zircon-syscall",
    "zircon-loader",
    "linux-object",
    "linux-syscall",
    "linux-loader",
    "kernel-hal",
    "drivers",
]

exclude = [
    "zircon-user",
    "zCore",
    "rboot",
]
