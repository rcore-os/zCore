[workspace]
members = [
    "zircon-object",
    "zircon-syscall",
    "zircon-loader",
    "linux-syscall",
    "linux-loader",
    "kernel-hal-unix",
    "kernel-hal-bare",
    "kernel-hal",
    "kernel-hal-bare",
    "kernel-hal-bare",
]

exclude = [
	"zCore",
	"rboot",
]
