name: Test CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  rust_toolchain: nightly-2026-09-01

jobs:
  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - name: Run unit test
        run: |
          python3 -m unittest discover -s scripts/tests
          cargo test --no-fail-fast
          cargo test -p zircon-object --lib --features libos,aspace-separate
          cargo test -p zircon-syscall --features libos,zircon-object/aspace-separate
          cargo test --manifest-path vendor/preemptive-scheduler/Cargo.toml --lib

  zircon-core-test-libos:
    name: Zircon Core Test Libos (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: x86_64 Linux
            os: ubuntu-latest
            arch: x86_64
            uname: x86_64
          - name: aarch64 Linux
            os: ubuntu-24.04-arm
            arch: aarch64
            uname: aarch64
          - name: aarch64 macOS
            os: macos-15
            arch: aarch64
            uname: arm64
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Verify native architecture
        run: test "$(uname -m)" = "${{ matrix.uname }}"

      - name: Install wget on macOS
        if: runner.os == 'macOS'
        run: brew install wget

      - name: Pull prebuilt images
        run: make zircon-init

      - name: Install python dependencies
        run: python3 -m pip install -r tests/requirements.txt

      - name: Verify native AArch64 fncall ABI
        if: matrix.arch == 'aarch64'
        run: |
          mkdir -p target
          cargo metadata --format-version 1 > target/cargo-metadata.json
          trapframe_manifest=$(python3 -c 'import json; print(next(p["manifest_path"] for p in json.load(open("target/cargo-metadata.json"))["packages"] if p["name"] == "trapframe"))')
          cargo test --manifest-path "$trapframe_manifest" --features fncall-preserve-x18 --release

      - name: Build macOS LibOS and report runtime skip
        if: runner.os == 'macOS'
        run: |
          make -C zCore ARCH=aarch64 LIBOS=1 TEST=1 ZBI=core-tests build
          mkdir -p target/test-logs/zircon-aarch64-libos
          python3 - <<'PYTHON'
          import json
          from pathlib import Path
          reason = "Darwin clears guest x18 on host exception return; GitHub macOS runners reject the required private custom-x18-abi entitlement (CI 34018139132). Fuchsia shadow-call-stack guests cannot start."
          Path("target/test-logs/zircon-aarch64-libos/results.json").write_text(json.dumps({"status": "SKIPPED", "reason": reason, "runs": []}, indent=2) + "\n")
          print("::warning::SKIP macOS Fuchsia runtime: " + reason)
          PYTHON

      - name: Run fast tests
        if: github.event_name != 'workflow_dispatch' && runner.os != 'macOS'
        run: python3 scripts/zircon_core_test.py --arch ${{ matrix.arch }} --libos --fast

      - name: Run full tests
        if: github.event_name == 'workflow_dispatch' && runner.os != 'macOS'
        run: python3 scripts/zircon_core_test.py --arch ${{ matrix.arch }} --libos

      - name: Upload guest and kernel logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zircon-core-test-libos-${{ matrix.arch }}-${{ runner.os }}
          path: |
            target/test-logs/
            target/kernel-*.log
            tests/*.log
            tests/kernel.log
          if-no-files-found: warn

  zircon-core-test:
    name: Zircon Core Test Baremetal (${{ matrix.arch }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            qemu_package: qemu-system-x86
            qemu_binary: qemu-system-x86_64
          - arch: aarch64
            qemu_package: qemu-system-arm
            qemu_binary: qemu-system-aarch64
          - arch: riscv64
            qemu_package: qemu-system-misc
            qemu_binary: qemu-system-riscv64
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Pull prebuilt images
        run: make zircon-init

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.qemu_package }}
          ${{ matrix.qemu_binary }} --version

      - name: Install cargo-binutils
        if: matrix.arch == 'riscv64'
        run: cargo install cargo-binutils --locked

      - name: Install python dependencies
        run: python3 -m pip install -r tests/requirements.txt

      - name: Run x64 SMP port stress regressions
        if: matrix.arch == 'x86_64'
        run: |
          python3 scripts/zircon_core_test.py --arch x86_64 --smp 2 --log-dir target/test-logs/port-smp2 --x64-cpu 'Haswell,+smap,-check,+fsgsbase,-x2apic' --group port-stress
          python3 scripts/zircon_core_test.py --arch x86_64 --smp 4 --log-dir target/test-logs/port-smp4 --group port-stress

      - name: Run fast tests
        if: github.event_name != 'workflow_dispatch'
        run: python3 scripts/zircon_core_test.py --arch ${{ matrix.arch }} --fast

      - name: Run full tests
        if: github.event_name == 'workflow_dispatch'
        run: python3 scripts/zircon_core_test.py --arch ${{ matrix.arch }}

      - name: Upload guest and kernel logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zircon-core-test-${{ matrix.arch }}
          path: |
            target/test-logs/
            target/kernel-*.log
            tests/*.log
            tests/kernel.log
          if-no-files-found: warn

  linux-libc-test-libos:
    name: Linux Libc Test Libos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - name: Prepare rootfs
        run: cargo xtask libos-libc-test

      - name: Install python dependencies
        run: pip3 install -r tests/requirements.txt

      - name: Run fast tests
        if: github.event_name != 'workflow_dispatch'
        run: python3 scripts/linux_libc_test.py --libos --fast

      - name: Run full tests
        if: github.event_name == 'workflow_dispatch'
        run: python3 scripts/linux_libc_test.py --libos

      - name: Upload guest and kernel logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-libc-test-libos-x86_64
          path: |
            target/test-logs/
            target/kernel-*.log
            tests/*.log
            tests/kernel.log
          if-no-files-found: warn

  linux-libc-test-baremetal:
    name: Linux Libc Test Baremetal
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, riscv64, aarch64]
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system
          qemu-system-${{ matrix.arch }} --version

      - name: Prepare rootfs
        run: cargo libc-test --arch ${{ matrix.arch }} && cargo image --arch ${{ matrix.arch }}

      - name: Install cargo-binutils
        if: matrix.arch == 'riscv64'
        run: cargo install cargo-binutils --locked

      - name: Install python dependencies
        run: pip3 install -r tests/requirements.txt

      - name: Run fast tests
        if: github.event_name != 'workflow_dispatch'
        run: python3 scripts/linux_libc_test.py --arch ${{ matrix.arch }} --fast

      - name: Run full tests
        if: github.event_name == 'workflow_dispatch'
        run: python3 scripts/linux_libc_test.py --arch ${{ matrix.arch }}

      - name: Upload guest and kernel logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-libc-test-baremetal-${{ matrix.arch }}
          path: |
            target/test-logs/
            target/kernel-*.log
            tests/*.log
            tests/kernel.log
          if-no-files-found: warn

  linux-other-test-baremetal:
    name: Linux Other Test Baremetal
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, riscv64, aarch64]
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.rust_toolchain }}
          components: rust-src, llvm-tools-preview

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system
          qemu-system-${{ matrix.arch }} --version

      - name: Prepare rootfs
        run: cargo other-test --arch ${{ matrix.arch }} && cargo image --arch ${{ matrix.arch }}

      - name: Install cargo-binutils
        if: matrix.arch == 'riscv64'
        run: cargo install cargo-binutils --locked

      - name: Install python dependencies
        run: pip3 install -r tests/requirements.txt

      - name: Run fast tests
        if: github.event_name != 'workflow_dispatch'
        run: cd tests && python3 linux_other_test.py --arch ${{ matrix.arch }} --fast

      - name: Run full tests
        if: github.event_name == 'workflow_dispatch'
        run: cd tests && python3 linux_other_test.py --arch ${{ matrix.arch }}
      - name: Upload guest and kernel logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-other-test-baremetal-${{ matrix.arch }}
          path: |
            target/test-logs/
            target/kernel-*.log
            tests/*.log
            tests/kernel.log
          if-no-files-found: warn
